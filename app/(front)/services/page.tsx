import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services",
};

const services = () => {
  return <div className="container">services page</div>;
};

export default services;
